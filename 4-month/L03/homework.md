# L36 Homework — Relational Databases & PostgreSQL Intro

**Student name:**
**Date:**
**Lesson date:**

---

## Questions

1. Explain what a **relational database** is and why the word "relational" is in the name. What is a **foreign key**, and how does it create a relationship between two tables? Give a concrete example using two tables from real life — not from the lesson examples.

2. Compare storing application data in **JSON files** (like you did in Month 2) versus storing it in a **PostgreSQL database**. What are the specific limitations of JSON files that become serious problems when your app has many users or large amounts of data? What does PostgreSQL solve that JSON cannot?

3. PostgreSQL has many data types for numbers: `INTEGER`, `NUMERIC`, `REAL`, `BIGINT`. Explain why you should **never use `REAL` or `FLOAT` to store money**. What problem does this cause, and which type should you use instead? Why?

4. The `NOT NULL`, `UNIQUE`, `PRIMARY KEY`, `FOREIGN KEY`, and `CHECK` constraints are all defined at the database level, not in your Python code. Why is it important to enforce these rules in the database itself, rather than just validating data in Python before inserting it?

---

## Reflection

1. What was the hardest part of this lesson for you?

2. Write one real situation from your daily life where you could use what you learned today.

3. What question do you still have that was not answered in the lesson?

