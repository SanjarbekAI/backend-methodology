# L39 Homework — SQL: Aggregations, GROUP BY & Subqueries

**Student name:**
**Date:**
**Lesson date:**

---

## Questions

1. Explain the difference between `WHERE` and `HAVING` in SQL. Why can you not use `WHERE` to filter on `COUNT(*)` or `SUM()`? Walk through a concrete example — such as "find product categories with more than 10 items" — and explain at which stage of query execution each clause operates.

2. `COUNT(*)`, `COUNT(column_name)`, and `COUNT(DISTINCT column_name)` each behave differently. Describe exactly what each one counts, and give a realistic scenario where using the wrong version would produce a silently incorrect result that looks plausible.

3. Explain what a **subquery** is and describe two different places where a subquery can appear in a SQL statement (e.g., in `WHERE`, in `FROM`). For each placement, give a real business question that it answers — something a manager or analyst would actually ask.

4. A **CTE** (`WITH ... AS (...)`) and a nested subquery in `FROM` can often produce identical results. Explain the differences in terms of readability, reusability within the same query, and maintainability. When would you choose a CTE over a subquery, and why?

---

## Reflection

1. What was the hardest part of this lesson for you?

2. Write one real situation from your daily life where you could use what you learned today.

3. What question do you still have that was not answered in the lesson?

