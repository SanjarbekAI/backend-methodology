# L28 Homework — Practice: OOP

**Student name:**
**Date:**
**Lesson date:**

---

## Questions

1. In today's project, `Room` is an abstract class while `Booking` is a dataclass. Explain why each design choice was made. What criteria did you use (or should use) to decide whether a domain object should be a class with methods, a dataclass, or an abstract class?

2. The `Hotel` class acts as an orchestrator — it calls other classes but contains no business logic itself. This follows SRP. Reflect on this design: what are the benefits of keeping the orchestrator "thin"? What would happen to the codebase if you put all the logic into `Hotel` directly?

3. The `PricingStrategy` hierarchy follows OCP. Describe a real hotel scenario where you would need to add a new pricing rule. With the OCP design in place, what exactly would you need to write — and more importantly, what existing code would you NOT need to touch? Why does this matter?

4. Looking back at L23 through L27, identify which OOP concept caused you the most design-level thinking during this project (not just implementation difficulty). Explain why that concept was challenging, how you resolved it, and what you would do differently in a future project.

---

## Reflection

1. What was the hardest part of this lesson for you?

2. Write one real situation from your daily life where you could use what you learned today.

3. What question do you still have that was not answered in the lesson?

